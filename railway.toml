[build]
builder = "DOCKERFILE"
dockerfilePath = "railway/Dockerfile.mlflow"

[deploy]
# No need to specify startCommand - Dockerfile CMD will be used
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
